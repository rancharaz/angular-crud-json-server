/* assigning types to reuse */
export interface SignUp {
    name:String,
    password:String,
    email:String
}

/* interface for datatype for login */
export interface login {
    email:String, 
    password:String
}